# Mario fMRI Tutorial Dependencies
# Python 3.9+
#
# Upper bounds are tight (major-version caps) for reproducibility.
# A lockfile at `requirements.lock` pins exact versions if you need a
# bit-for-bit reproduction of the tutorial's reference environment.

# Core scientific computing
numpy>=1.21.0,<2.0.0
scipy>=1.7.0,<2.0.0
pandas>=1.3.0,<3.0.0

# Neuroimaging
nibabel>=4.0.0,<6.0.0
nilearn>=0.10.0,<1.0.0

# Machine learning
scikit-learn>=1.0.0,<2.0.0
torch>=2.0.0,<3.0.0

# Visualization
matplotlib>=3.5.0,<4.0.0
seaborn>=0.12.0,<1.0.0

# Jupyter + slideshows
jupyter>=1.0.0,<2.0.0
notebook>=6.0.0,<7.0.0   # Classic notebook interface (v6.x) — required by RISE
RISE>=5.7.0,<6.0.0       # Classic RISE extension for slideshow presentations
ipython>=8.0.0,<10.0.0

# RL environment (stable-retro = maintained fork of gym-retro)
stable-retro>=0.9.0,<1.0.0

# Progress bars
tqdm>=4.60.0,<5.0.0

# Image processing
opencv-python>=4.5.0,<5.0.0

# Dataset management
datalad>=0.15.0,<2.0.0
gdown>=5.0.0,<6.0.0
